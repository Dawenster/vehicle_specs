.call-to-action-holder
  .btn.btn-dark
    | Save to PDF

  .btn.btn-dropdown ng-show="showBuilder" ng-click="preview()"
    | Preview

  .btn.btn-dropdown ng-show="!showBuilder" ng-click="backToBuild()"
    i.fa.fa-angle-left
    | Back to Build